using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Data;

namespace IMES.Query.Interface.QueryIntf
{
    public interface ISA_SABANDIT
    {
        DataTable GetQueryResult(string Connection, DateTime FromDate, DateTime ToDate,
                 string Family);
        DataTable GetSelectDetail(string Connection, DateTime FromDate, DateTime ToDate,
                 string Family, string mode);
        DataTable GetQueryResult1(string Connection, DateTime FromDate, DateTime ToDate,
                string Family);
        DataTable GetSelectDetail1(string Connection, DateTime FromDate, DateTime ToDate,
                 string Family, string mode);
    }
}
