using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using IMES.Infrastructure.Repository._Metas;

namespace IMES.DataModel
{
    [ORMapping(typeof(UPSIECPO))]
    [Serializable]
    public class UPSIECPOInfo
    {
        [ORMapping(UPSIECPO.fn_id)]
        public Int64 ID = long.MinValue;

        [ORMapping(UPSIECPO.fn_iecpo)]
        public String IECPO = null;

        [ORMapping(UPSIECPO.fn_iecpoitem)]
        public String IECPOItem = null;

        [ORMapping(UPSIECPO.fn_model)]
        public String Model = null;

        [ORMapping(UPSIECPO.fn_hppo)]
        public String HPPO = null;

        [ORMapping(UPSIECPO.fn_qty)]
        public int Qty = int.MinValue;

        [ORMapping(UPSIECPO.fn_editor)]
        public String Editor = null;

        [ORMapping(UPSIECPO.fn_cdt)]
        public DateTime Cdt = DateTime.MinValue;

        [ORMapping(UPSIECPO.fn_udt)]
        public DateTime Udt = DateTime.MinValue;      
    }
}
